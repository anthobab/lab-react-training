import React from 'react';
import './NubersTable.css';

const NubersTable = ({ limit }) => {
  let returnedStr = '';
  //   for (let index = 1; index <= limit; index++) {
  //     returnedStr += '<div className='+ ((index%2) ?  '"redCase" >' : )+ index +'</div>'  ;
  //   }

  return <div className="NumbersTable">{}</div>;
};

export default NubersTable;
